<div class="">
<ul class="pt-2 menu">

 <li class="!pb-2  menu-title">
    <span class="!text-base !font-medium capitalize">{l "Your topics"}</span>
  </li>
  <li :for={topic <- e(@topics, [])}>
    <LiveRedirect 
      to={category_link(e(topic, :edge, nil))} 
      class={"rounded text-sm relative"}
    >
      <Solid.CollectionIcon class={"w-5 h-5 text-base-content/80"} />
      {e(topic, :edge, :object, :profile, :name, "")}
    </LiveRedirect>
  </li>
  
  <!-- WIP: Add the max amount of topics to show as a config -->
  <div 
    :if={length(e(@topics, [])) > 5}  
    class="inline mt-2">
    <button class="justify-start text-left normal-case btn btn-ghost btn-sm">
      <span>{l "More"}</span>
      <Solid.ChevronDownIcon class="w-4 h-4 ml-2" />
    </button>
  </div>
</ul>
</div>