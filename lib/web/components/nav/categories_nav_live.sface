<div class="px-0 py-4 mt-2 ml-4 text-sm font-medium tracking-wide text-base-content/60">{l("Favourited topics")}</div>
<ul class="space-y-1 menu menu-compact">
  <li :for={%{edge: %{object: topic}} <- e(@topics, [])}>
    <LiveRedirect to={category_link(topic)} class="flex items-center gap-5 rounded-md">
      <Icon solid="Collection" class="w-5 h-5 text-base-content/80" />
      {e(topic, :profile, :name, "")}
    </LiveRedirect>
  </li>
</ul>
